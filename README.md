# Dust Investment Agent 2.0 – Flat Version

Alle Dateien werden direkt in die oberste Ebene des GitHub-Repositories hochgeladen.

Startdatei in Streamlit: `app.py`

Die echten API-Schlüssel ausschließlich in Streamlit unter App settings → Secrets eintragen:

```toml
OPENAI_API_KEY = "sk-..."
FMP_API_KEY = "..."
OPENAI_MODEL = "gpt-5-mini"
```
